import React from 'react'
import { useDispatch, useSelector } from 'react-redux';
type Props = {}

const Projects = (props: Props) => {
  // const isDarkMode = useSelector((state: any) => state.theme.isDarkMode);


  // return (    
  //   <div id='projects' className={`transition-all duration-500 ${isDarkMode ? 'bg-dark text-white ' : 'bg-alternative-white text-black'} w-full min-h-min z-40 flex flex-row justify-center`}>

  //   Projects</div>
  // )
}

export default Projects;